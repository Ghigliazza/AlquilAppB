<%= render @rental %>

<%# @tiempo = @rental.expires - Date.today %>
<%# Renderiza el partial del timer - enviando el tiempo en minutos %>
<%= render partial: 'rentals/timer', locals: { tiempo: ((@rental.expires - Time.now)/1.seconds).round} %>

<div>
  <% if !@rental.expired? %>
    <%= link_to "Extender Alquiler", edit_car_rental_path(@rental.car), class: "btn btn-primary", id: "extendRentBtn" %>
    <%= link_to "Cancelar Alquiler", cancel_rental_path, class: "btn btn-danger", id: "cancelRentBtn" %>
  <% end %>

  <%= link_to "Reportar", new_rental_report_path(@rental), class: "btn btn-warning", id: "reporttBtn" %>

  <% if current_user.admin? %>
    <%= button_to "Eliminar Alquiler", @rental, method: :delete, class: "btn btn-danger"  %>
  <% end %>
</div>

++++++++++++++  RENTAL  ++++++++++++++<br>
ID: <%= @rental.id %><br>
PRICE: <%= @rental.price %><br>
EXPIRE: <%= @rental.expires %><br>
STATE: <%= @rental.state %><br>

++++++++++++++  CAR  ++++++++++++++<br>
<% car = @rental.car %><br>
ID: <%= car.id %><br>


++++++++++++++  TIME  ++++++++++++++<br>
TIEMPO EN SEG DEL ALQUILER: <%= ((@rental.expires - Time.now)/1.seconds).round %><br>